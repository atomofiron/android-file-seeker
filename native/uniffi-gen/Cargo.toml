[package]
name = "uniffi-gen"
version = "0.1.0"
edition = "2021"

[dependencies]
uniffi = { version = "0.29.4", features = ["cli"] }

[build-dependencies]
uniffi = { version = "0.29.4", features = [ "build" ] }
